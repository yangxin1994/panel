# Using Typescript and JSX with Panel - Type checking on attributes

To install and run the example from this directory: `npm install && npm start`. The page will be served on `localhost:8080` by Webpack dev server.
